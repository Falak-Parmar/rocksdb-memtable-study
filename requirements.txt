matplotlib>=3.5.0
numpy>=1.21.0
pandas>=1.3.0
psutil>=5.9.0
# python-rocksdb is needed for exp3 write-stall experiment
# Install separately if pip install fails:
#   conda install -c conda-forge python-rocksdb
# or build from source: https://github.com/twmht/python-rocksdb
python-rocksdb>=0.7.0
